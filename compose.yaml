services:
  backend:
    image: backend
    build:
      context: backend/
    ports:
      - 3001:3001
  frontend:
    # For pre-rendering nextjs pages
    depends_on:
      - backend
    image: frontend
    build:
      context: frontend/
  caddy:
    image: caddy
    build:
      context: ./caddy/
    ports:
      - 80:80
    develop:
      watch:
        - path: ./caddy/
        - action: rebuild
